import { GuildMember, MessageEmbed } from 'discord.js';

export default class ModLogger {
    constructor() {

    }

    log(action: string, target: GuildMember, author: GuildMember, reason: string) {

    }
}